<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_dark">

    <!-- الهيدر -->
    <LinearLayout
        android:id="@+id/headerBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="18dp"
        android:layout_margin="10dp"
        android:background="@drawable/bg_card">

        <TextView
            android:id="@+id/headerTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⚔️ IRAQ B7R"
            android:textColor="@color/accent_gold"
            android:textSize="24sp"
            android:textStyle="bold"
            android:letterSpacing="0.08" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="ALLIANCE TIMING SYSTEM"
            android:textColor="@color/text_sub_dark"
            android:textSize="10sp"
            android:letterSpacing="0.15" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <include
            android:id="@+id/viewCars"
            layout="@layout/view_cars" />

        <include
            android:id="@+id/viewArchive"
            layout="@layout/view_archive"
            android:visibility="gone" />

        <include
            android:id="@+id/viewSettings"
            layout="@layout/view_settings"
            android:visibility="gone" />
    </FrameLayout>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/card_bg_dark"
        app:itemIconTint="@color/accent_gold"
        app:itemTextColor="@color/accent_gold"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
